from django.contrib import admin
from django.urls import path, re_path
from users.views import signup
#
urlpatterns = [
    # path(r'myuser/',viewMyUser.as_view()),
    # re_path(r'signup/$',signup, name = 'signup'),

]